name: testing

on:
  schedule:
    - cron: '0 */2 * * *'
  workflow_dispatch:

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:      
    - name: hello
      uses: hooopo/repo-track-pipeline@v1.0.4
      with:
        github-token: ${{ secrets.GITHUB_TOKEN }} 
        database_url: ${{ secrets.DATABASE_URL }}
        repo-full-name: ${{ secrets.REPO_FULL_NAME }}
